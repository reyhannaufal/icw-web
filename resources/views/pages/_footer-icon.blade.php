<a href="{{ $route }}" class="text-gray-400 hover:text-gray-500">
    <span class="sr-only">{{ $name }}</span>
    <svg
        class="h-6 w-6"
        fill="currentColor"
        viewBox="0 0 24 24"
        aria-hidden="true"
    >
        <path
            fill-rule="evenodd"
            d="{{ $path }}"
            clip-rule="evenodd"
        ></path>
    </svg>
</a>
